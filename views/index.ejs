<!-- index.ejs -->
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Accueil – Port de Plaisance Russell</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header') %>

<h1>Bienvenue au Port de Plaisance Russell</h1>

<p>
  Cette application permet à la capitainerie de gérer les catways (emplacements pour bateaux), les utilisateurs et leurs réservations.
  Grâce à une API sécurisée et une interface intuitive, simplifiez vos opérations portuaires.
</p>

<% if (!user) { %>
  <h2>Connexion</h2>
  <form method="POST" action="/" autocomplete="off">
    <input type="text" name="name" placeholder="Nom d’utilisateur" autocomplete="username" required value="<%= user?.name || '' %>"><br>
    <input type="password" name="password" placeholder="Mot de passe" autocomplete="current-password" required value=""><br>
    <button type="submit">Se connecter</button>
  </form>

  <% if (typeof message !== 'undefined' && message.length > 0) { %>
    <p style="color: red;"><%= message %></p>
  <% } %>

  <p>Pas encore de compte ? <a href="/register">Créer un compte</a></p>

<% } else { %>
  <h2>Bienvenue <%= user.name %> !</h2>
  <p><a href="/dashboard">Accéder au tableau de bord</a> | <a href="/logout">Déconnexion</a></p>
<% } %>

<p><a href="/documentation">📄 Voir la documentation de l’API</a></p>

<%- include('partials/footer') %>
</body>
</html>